{
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.3",
    "@tailwindcss/typography": "^0.5.8",
    "tailwindcss": "^3.2.4"
  },
  "scripts": {
    "preinstall": "cargo build",
    "start": "trunk serve --port 8003",
    "serve": "trunk serve --port 8003 --release"
  }
}
